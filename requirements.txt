# Requires Python 3.13 or older — crewai does NOT support Python 3.14.
# This project is pinned/tested against Python 3.12.

# --- Runtime ---
crewai==0.80.0                 # agent framework for the file-inspection path
python-telegram-bot==22.8      # Telegram Bot API front-end
python-dotenv==1.2.2           # load GEMINI_API_KEY / TELEGRAM_BOT_TOKEN from .env
PyYAML==6.0.3                  # reference data + evaluation set (yaml.safe_load)
httpx==0.28.1                  # streaming file downloads + VirusTotal API calls
pyaxmlparser==0.3.31           # static AndroidManifest.xml parsing (no execution)

# setuptools must stay <81: crewai 0.80.0 imports pkg_resources,
# which was removed from setuptools 81+.
setuptools<81

# --- Dev / testing ---
pytest==9.1.1